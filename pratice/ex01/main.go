package main

/*
## 題組 1：智慧溫室感測紀錄（01）

目標：練習變數宣告、基本型別、零值、重新賦值與格式化輸出。

情境：
溫室控制系統每小時會記錄區域名稱、溫度、濕度與設備是否運轉。
請建立一筆感測紀錄，並模擬下一小時的數值變化。

核心要求：

1. 使用 `var zone string` 宣告區域名稱，再於下一行指定內容。
2. 使用型別推斷 `var temperature = ...` 宣告浮點數溫度。
3. 使用短宣告 `humidity := ...` 宣告整數濕度百分比。
4. 使用 bool 記錄風扇是否運轉。
5. 另外宣告未初始化的 int、float64、string、bool，印出各自零值；
   空字串必須使用 `%q` 顯示。
6. 使用一次 `fmt.Printf` 印出完整紀錄：區域、溫度、濕度及風扇狀態。
7. 溫度顯示一位小數，濕度後面顯示 `%`。
   提示：若要在 Printf 印出百分比符號，格式字串需寫成 `%%`。
8. 模擬下一小時：修改溫度、濕度及風扇狀態，再輸出第二筆紀錄。

資料設計：

- 區域名稱和感測數字由你自行決定。
- 第二筆資料至少要有兩個欄位和第一筆不同。

自行驗收：

- 程式使用了 var 分離賦值、var 型別推斷及 `:=` 三種宣告方式。
- 零值應包含 `0`、`0.0`、`""`、`false`。
- 兩筆紀錄的溫度均固定顯示一位小數。
- 輸出的百分比符號不會被誤認為格式化動詞。

思考題：

1. 省略 temperature 的型別後，Go 如何決定它是 float64？
2. `=` 和 `:=` 有什麼差異？
3. 為什麼 `%q` 比 `%s` 更適合觀察空字串？

執行方式（專案根目錄）：go run ./pratice/ex01
*/

func main() {
	// TODO: 以三種指定方式宣告第一筆感測資料。

	// TODO: 宣告並印出四種型別的零值。

	// TODO: 使用 fmt.Printf 印出第一筆紀錄。

	// TODO: 更新至少兩個欄位，再印出第二筆紀錄。
}
